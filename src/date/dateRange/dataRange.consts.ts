// dateRange.consts.ts

export const DATE_RANGE_MESSAGES = {
  invalidStart: "Invalid start date",
  invalidEnd: "Invalid end date",
  startBeforeEnd: "Start date must be before end date",
} as const;